apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: otel-sandbox-apps

commonLabels:
  app.kubernetes.io/part-of: otel-sandbox

resources:
- backend/
- frontend.yaml
- consumer.yaml
